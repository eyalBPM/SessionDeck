<UserControl x:Class="SessionDeck.TaskItemView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- One task card (T-0116): name + optional status/description, action buttons.
         DataContext = TaskItemViewModel. Used by the tasks page and by the workspace
         card's inline task list. -->
    <Border BorderBrush="{Binding StatusBrush}" BorderThickness="2" CornerRadius="5"
            Margin="0,3" Padding="8,5" Background="#FF262626"
            ToolTip="{Binding TooltipText}">
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <DockPanel FlowDirection="{Binding Name, Converter={StaticResource Rtl}}">
                    <TextBlock DockPanel.Dock="Left" Text="📌" FontSize="10" Margin="0,1,4,0"
                               VerticalAlignment="Top">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Pinned}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding Name}" Foreground="White" FontSize="12"
                               FontWeight="SemiBold" TextWrapping="Wrap"/>
                </DockPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Top"
                            Margin="8,0,0,0">
                    <TextBlock Text="{Binding Status}" Foreground="{Binding StatusBrush}" FontSize="11"
                               VerticalAlignment="Center">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding HasStatus}" Value="False">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <TextBlock Text="{Binding Id}" Foreground="#FF777777" FontSize="10"
                               VerticalAlignment="Center" Margin="8,0,0,0"/>
                </StackPanel>
            </Grid>
            <TextBlock Text="{Binding Description}" Foreground="#FF999999" FontSize="10.5"
                       TextWrapping="Wrap" Margin="0,2,0,0"
                       FlowDirection="{Binding Description, Converter={StaticResource Rtl}}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding HasDescription}" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,0" FlowDirection="RightToLeft">
                <StackPanel.Resources>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#FF333333"/>
                        <Setter Property="Foreground" Value="#FFDDDDDD"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Padding" Value="9,2"/>
                        <Setter Property="Margin" Value="0,0,6,0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="FontSize" Value="11"/>
                    </Style>
                </StackPanel.Resources>
                <Button x:Name="TargetButton" Content="{Binding TargetButtonText}"
                        Click="Target_Click"
                        ToolTip="פתיחת סשן במשימה — סשן קיים או חדש ב-workspace שלה">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasTarget}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
                <Button Content="🔗 פתח משימה" Click="Url_Click"
                        ToolTip="{Binding Url}">
                    <Button.Style>
                        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HasUrl}" Value="False">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
