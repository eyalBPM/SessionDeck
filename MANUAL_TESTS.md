# WinGrid — רשימת בדיקות ידניות (v0.2.0)

> נכתב 2026-07-16 בסיום מימוש שלב ב'. הבדיקות האוטומטיות (CLI end-to-end, zone/work-area, ‏re-bind, persistence, הבהוב ויזואלי דרך snapshot) כבר עברו — הרשימה כאן מכסה את מה שדורש נוכחות פיזית מול המסך: אינטראקציות עכבר, ריבוי מסכים, ותחושת שימוש.
>
> **הרצה:** `bin\Debug\net10.0-windows\WinGrid.exe` (או F5 מ-Visual Studio).
> **CLI מטרמינל:** `WinGrid.exe <command>` מאותה תיקייה (`WinGrid.exe help` לרשימה המלאה).
> **קובץ ה-config:** `%APPDATA%\WinGrid\config.json` — אפשר למחוק אותו כדי להתחיל נקי.

## 1. Grid + תצוגה חיה (שלב א')

- [ ] פתח 2–3 חלונות VSCode אמיתיים והוסף אותם דרך ה-Picker (גרור את ⌖ מהסרגל אל חלון ושחרר).
- [ ] ודא תצוגה חיה: הקלד בתוך VSCode — ה-thumbnail מתעדכן בזמן אמת.
- [ ] ודא שה-thumbnail חי גם כשהחלון המקורי מכוסה ע"י חלון אחר (לא ממוזער).
- [ ] מזער חלון מקור — ה-thumbnail יקפא על הפריים האחרון (התנהגות מתועדת, SPEC §6.1).
- [ ] שנה את גודל חלון WinGrid — הפריסה מתעדכנת, יחס תצוגה של כל thumbnail נשמר (letterboxing).
- [ ] ה-Picker מסרב לחלונות לא-תקפים: WinGrid עצמו, חלון בלי כותרת — הודעה ב-status bar.
- [ ] ניסיון להוסיף חלון שכבר ב-grid → הודעת "כבר נמצא ב-grid".

## 2. סדר בגרירה — ~~הוסר~~ (2026-07-17)

גרירת tiles הוסרה (v0.2.1) בעקבות משוב אייל: לא רוצים שינוי מקום בגרירה, וגרירה גרמה להקפאה. לחיצה = Focus, double-click = פתיחה באזור הפתיחה.

## 3. Focus / Pin / Stage (שלב א'+ב')

- [ ] לחיצה על tile → החלון האמיתי קופץ לחזית **במיקומו הנוכחי**.
- [ ] כפתור ▶ → החלון זז ל"אזור הפתיחה" (Stage) המוגדר בסרגל ומופעל.
- [ ] **Double-click על tile → Pin** (שלב ב').
- [ ] שנה את ה-Stage מהסרגל (מסך + מלא/חצי) ו-Pin שוב — החלון נוחת באזור הנכון.
- [ ] Stage על המסך השני — ‏Pin מעביר חלון בין מסכים כולל DPI שונה.
- [ ] `stage --rect 100,100,800,600` מה-CLI ואז Pin — החלון נוחת במלבן המדויק.

## 4. Reserved Zone (שלב א')

- [ ] הפעל Zone "חצי ימין" על המסך הראשי — חלון WinGrid נצמד לחצי הימני.
- [ ] נסה למקסם חלון אחר — הוא מתמקסם רק עד גבול ה-Zone (לא נכנס מתחת).
- [ ] נסה Snap (Win+חץ) של חלון אחר — לא פולש ל-Zone; העכבר עובר חופשי.
- [ ] עבור בין המצבים: חצי שמאל / מסך מלא / כבוי — המעבר חלק, וב"כבוי" החלון חוזר למיקומו הקודם.
- [ ] ‏Zone על המסך השני (ultrawide) — עובד per-monitor.
- [ ] סגור את האפליקציה כש-Zone פעיל — ה-work area משוחרר (חלונות שוב מתמקסמים מלא).

## 5. הבהוב (שלב ב')

- [ ] `WinGrid.exe border 1 --color orange --alt black` — המסגרת מהבהבת כתום/שחור בקצב 500ms.
- [ ] `--interval 200` — קצב מהיר יותר, נראה לעין.
- [ ] שני tiles מהבהבים בקצבים שונים בו-זמנית — כל אחד בקצב שלו.
- [ ] `border 1 --color green` (בלי ‎--alt) — ההבהוב נפסק, צבע סטטי.

## 6. עריכה ב-UI (שלב ב')

- [ ] כפתור ✏ פותח את דיאלוג העריכה.
- [ ] שינוי כותרת ידנית → הכותרת לא מתעדכנת יותר משינויי title של החלון.
- [ ] סימון "כותרת אוטומטית" מחזיר את הכותרת החיה מהחלון.
- [ ] תיאור מופיע על ה-tile (שורה אפורה מתחת לכותרת) וב-tooltip.
- [ ] צבע לא חוקי → הדיאלוג חוסם עם הודעה; ה-preview מתעדכן תוך כדי הקלדה.
- [ ] הגדרת הבהוב מהדיאלוג (צבע משני + קצב) עובדת.

## 7. Drag-in (שלב ב' — לא נבדק בכלל, דורש עכבר פיזי)

- [ ] גרור חלון אמיתי (ב-title bar שלו) ושחרר כשהסמן מעל חלון WinGrid — נוסף tile.
- [ ] גרור חלון שכבר ב-grid — לא נוסף כפול.
- [ ] סגור חלון של tile (נהיה מנותק), פתח חלון תואם וגרור אותו פנימה — ה-tile הקיים מתחבר מחדש במקום להיווצר חדש.
- [ ] גרירת חלון שמסתיימת **מחוץ** ל-WinGrid — לא קורה כלום.

## 8. ניתוק ו-re-bind (שלב ב' — נבדק אוטומטית, כדאי אימות ויזואלי)

- [ ] סגור חלון של tile — ה-tile מציג "🔌 מנותק / ממתין לחלון תואם", הכותרת/צבע/תיאור נשמרים.
- [ ] פתח חלון חדש שתואם ל-Matcher — ה-tile מתחבר אוטומטית וה-thumbnail חוזר.
- [ ] הפעל "הסר אריח אוטומטית כשהחלון נסגר" (תפריט ⚙) וסגור חלון — ה-tile נמחק במקום להתנתק.

## 9. עלייה עם Windows (שלב ב' — לא נבדק)

- [ ] הפעל את הטוגל בתפריט ⚙ וודא שנוצר ערך registry: `HKCU\Software\Microsoft\Windows\CurrentVersion\Run\WinGrid`.
- [ ] הפעל מחדש את המחשב (או logoff/logon) — WinGrid עולה אוטומטית ומשחזר: tiles (מנותקים עד שהחלונות ייפתחו), Zone, Stage, סדר.
- [ ] כבה את הטוגל — הערך נמחק מה-registry.

## 10. Persistence כללי (נבדק אוטומטית, אימות מהיר)

- [ ] בצע כמה שינויים (צבע, סדר, תיאור) בלי "לשמור" — סגור והפעל מחדש — הכל שם.
- [ ] `%APPDATA%\WinGrid\config.json` קריא ומשקף את המצב.

## 11. מקרי קצה שכדאי להריץ פעם אחת

- [ ] חלון elevated (admin): thumbnail אמור לעבוד; Focus/Pin עשויים להיחסם (מגבלה מתועדת, SPEC §6.5).
- [ ] גרירת WinGrid בין מסכים עם DPI שונה — ה-thumbnails נשארים ממוקמים נכון.
- [ ] הפעלה שנייה של ה-exe בלי ארגומנטים — לא נפתח instance שני; החלון הקיים קופץ לחזית.
- [ ] `WinGrid.exe focus 1` מטרמינל כשהאפליקציה ברקע — ייתכן שהחלון רק יהבהב בשורת המשימות במקום לקפוץ (מגבלת anti focus-stealing של Windows, SPEC §6.2 — לא קריטי לשלב ג').

---

*הערה: הפקודה הפנימית `WinGrid.exe snapshot <path>.png` מרנדרת את ה-UI ל-PNG (בלי ה-thumbnails, שמצוירים ע"י ה-DWM) — שימושית לדיבוג מרחוק.*
